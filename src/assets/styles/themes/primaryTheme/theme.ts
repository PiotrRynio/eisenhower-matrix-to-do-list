import { Theme } from '../types';
import { breakpoints, fontSizes, fontWeights, lineHeights, sizes, commonMixins } from '../common';
import { colors, fontFamilies, themeMixins } from './themeValues';

export const theme: Theme = {
  breakpoints,
  colors,
  fontFamilies,
  fontSizes,
  fontWeights,
  lineHeights,
  commonMixins,
  themeMixins,
  sizes,
};
